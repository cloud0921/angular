import { MyCheckDirective } from './my-check.directive';

describe('MyCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new MyCheckDirective();
    expect(directive).toBeTruthy();
  });
});
